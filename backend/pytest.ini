[pytest]
testpaths = tests
filterwarnings =
    # Comes from inside Starlette's test client, not from ThreatLens code.
    # Remove this line once a newer Starlette release fixes it.
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning:starlette.testclient
